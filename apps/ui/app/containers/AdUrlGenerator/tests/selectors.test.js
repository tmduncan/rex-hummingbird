// import { fromJS } from 'immutable';
// import { makeSelectAdUrlGeneratorDomain } from '../selectors';

// const selector = makeSelectAdUrlGeneratorDomain();

describe('makeSelectAdUrlGeneratorDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
