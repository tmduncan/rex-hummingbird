// import { fromJS } from 'immutable';
// import { makeSelectUrlShortenerDomain } from '../selectors';

// const selector = makeSelectUrlShortenerDomain();

describe('makeSelectUrlShortenerDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
